uvicorn connection:app --host 0.0.0.0 --port 8000

curl -X POST "localhost:8000/start?sleep=10"

curl -X GET "localhost:8000/result"

curl -X POST "localhost:8000/stop"

curl -X POST "localhost:8000/push_url?url=https://example.com/video1"

curl -X POST "localhost:8000/delete_url?url=https://example.com/video1"

curl -X GET "localhost:8000/get_url"
